<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navbar">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ui.institutions.ANAF.ANAF" />
        <activity android:name=".ui.institutions.CJASI.CJASI" />
        <activity android:name=".ui.institutions.CJPI.CJPI" />
        <activity android:name=".ui.institutions.DASI.DASI" />
        <activity android:name=".ui.institutions.DGASPCI.DGASPCI" />
        <activity android:name=".ui.institutions.DGpP.DGpP" />
        <activity android:name=".ui.institutions.DLEPI.DLEPI" />
        <activity android:name=".ui.institutions.IPJI.IPJI" />
        <activity android:name=".ui.institutions.PI.PI" />
        <activity android:name=".ui.institutions.SALUBRIS.SALUBRIS" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="OnlineRouting.Key"
            android:value="U09l3qmGGiX8pFoYrlfka5e3k9woPmZf" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>